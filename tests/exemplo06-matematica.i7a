funcao principal
    a = a + 1; { Adicao simples }
    a = a - 1; { Subtracao simples }
    a = a * 1; { Multiplicacao simples }
    a = a / 1; { Divisao simples }
    a = -1; { Operacao unaria negativo de um numero }
    a = -a; { Operacao unaria negativo de uma variavel }
    a = a + a * a / a - 1; { Combinacao de adicao, multiplicacao, divisao e subtracao }
    a = -(1 + a); { Operacao unaria com parenteses }
    a = a * (-1); { Multiplicacao com operador unario negativo }
    a = (-a) * (-1); { Multiplicacao de dois operadores unarios negativos }
    a = 1 * (-(1 + 1)); { Multiplicacao com expressao unaria dentro de parenteses }
    a = -(a * 1); { Operacao unaria com multiplicacao }
    a = (a + b) - (c - d); { Uso de parenteses para alterar a precedencia }
    a = a * (b + c) / d; { Combinacao de multiplicacao, adicao e divisao com parenteses }
    a = a / (b - c) + d; { Combinacao de divisao, subtracao e adicao com parenteses }
    a = (-(a + b)) * c; { Operacao unaria com adicao e multiplicacao }
    a = a + b * (c - d) / e; { Combinacao de todas as quatro operacoes com parenteses }
    a = (a + b) * (c - d) / e; { Expressao com multiplos parenteses e operacoes }
    a = a + b * (c - d) / (-e); { Expressao com divisao por operador unario negativo }
    a = (a / (b + c)) + d; { Operacao unaria envolvendo divisao e parenteses }
    a = a * b - c / d + e; { Combinacao de multiplicacao, divisao, subtracao e adicao }
    a = (a - b) * (c + d) / (e - f); { Expressao com multiplos parenteses e operacoes }
    a = a * (b / (c + d)) - e; { Expressao com divisao dentro de parenteses e multiplicacao }
    a = (-((a + b) * c)) / d; { Operacao unaria envolvendo multiplicacao e divisao }
    a = a * b + c - d / e; { Combinacao de multiplicacao, adicao, subtracao e divisao }
    a = (a + (b - c)) * d; { Uso de parenteses aninhados }
    a = -a / b + (c * d); { ERRO Divisao com operador unario e multiplicacao dentro de parenteses }
    a = (a * -b) / (c - d) + e; { ERRO Multiplicacao e divisao com operadores unarios e parenteses }
    a = (a + -b) * (c - -d); { ERRO Uso de operadores unarios dentro de parenteses }
    a = (a - b) / (c + d) * -e; { ERRO Expressao com multiplicacao por operador unario negativo }
    a = -a + b * -c; { ERRO Operacoes unarias e multiplicacao }
    a = -a * -b; { ERRO Multiplicacao de duas variaveis com operadores unarios }
    a = a * -b + c; { ERRO Multiplicacao com operador unario seguido de adicao }
    a = a = a - 1; { ERRO Atribuicao encadeada com subtracao }
    a = a - -1; {ERRO Subtracao com operador unario negativo }
    a = a + ; { ERRO Operador + sem operando a direita }
    {olhar} a = * a + 1; { ERRO Operador * sem operando a esquerda }
    a = a + * 1; { ERRO Operadores + e * consecutivos sem operando intermediario }
    a = (a + 1; { ERRO Parenteses de abertura sem correspondente fechamento }
    a = a + 1); { ERRO Parenteses de fechamento sem correspondente abertura }
    a = a $ 1; { ERRO Operador invalido $ }
    a = a ++ 1; { ERRO Dois operadores + consecutivos }
    a = a * / 1; { ERRO Operadores * e / consecutivos }
    a = a + #; { ERRO Token invalido # }
    a = a + 1a; { ERRO Numero invalido 1a }
    a = --a; { ERRO Operadores unarios duplicados -- (se nao suportado) }
    a = a + -; { ERRO Operador - sem operando a direita }
    a = a + 1 +; { ERRO Operador + no final sem operando }
    a = a * ; { ERRO Operador * sem operando a direita }
    {OLHAR} a = ; { ERRO Ausencia de expressao apos = }
    {OLHAR} a + 1 = a; { ERRO Expressao invalida no lado esquerdo da atribuicao }
    {OLHAR} = a + 1; { ERRO Ausencia de variavel a esquerda do = }
    a = a 1; { ERRO Falta de operador entre a e 1 }
    a = a b + 1; { ERRO Falta de operador entre a e b }
    a = (a + b)) - (c - d; { ERRO Parenteses desbalanceados }
    a = a + (b + c; { ERRO Parenteses de fechamento ausente }
    a = (a + (b - c); { ERRO Parenteses de fechamento ausentes }
    a = ((a + b) * c; { ERRO Parenteses de fechamento ausente }
    {OLHAR} a == a + 1; { ERRO Uso do operador de igualdade == em vez de = }
    {OLHAR} a === a; { ERRO Operador de igualdade tripla === invalido }
    a = + ; { ERRO Operador + sem operando }
    a = - ; { ERRO Operador - sem operando }
    a = a / ; { ERRO Operador / sem operando a direita }
    a = a * ; { ERRO Operador * sem operando a direita }
    {OLHAR} a = ; { ERRO Ausencia de expressao apos = }
    a = ( ); { ERRO Expressao vazia dentro dos parenteses }
    a = (a + 1)) + (b - ); { ERRO Multiplos erros de parenteses e operadores }
    a = a + @ + 1; { ERRO Token invalido @ }
    a = a + 1!; { ERRO Token invalido ! }
    a = (a + (b * c)) + ); { ERRO Parenteses de fechamento extra ) }
    a = a + ((b * c) - d; { ERRO Parenteses de fechamento ausente }
    a = (); { ERRO Expressao vazia dentro dos parenteses }
    a = ( ); { ERRO Expressao vazia com espaco dentro dos parenteses }
    a = a ++ 1; { ERRO Dois operadores + consecutivos }
    a = a -- 1; { ERRO Dois operadores - consecutivos }
    a = a + * 1; { ERRO Operadores + e * consecutivos }
    a = a * / 1; { ERRO Operadores * e consecutivos }
    a = a + ; { ERRO Operador + sem operando }
    a = +a; { Operador + }
    a = a = a - 1; { ERRO Atribuicao encadeada nao suportada }
    {OLHAR} a == a; { ERRO Uso do operador de igualdade == em vez de = }
    a = "a + 1; { ERRO String nao encerrada }
    a = a + "1; { ERRO String nao encerrada }
    a = a / / b; { ERRO Dois operadores consecutivos }
    {OLHAR} a = ) a + 1; { ERRO Expressao comeca com parenteses de fechamento ) }
    a = a + 1; extra; { ERRO Tokens extras apos a expressao }
    {OLHAR} a = a + 1; ; { ERRO Ponto e virgula extra }
    {OLHAR} a = ; { ERRO Atribuicao sem expressao }
    a = (a + b)) + c; { ERRO Parenteses de fechamento extra ) }
    a = a + (b + c)) - d; { ERRO Parenteses de fechamento extra ) }
    a = (a + (b - c)); { Correto, mas incluido para verificar erro oculto }
    a = (a + b) * (c - d; { ERRO Parenteses de fechamento ausente }
    a = a + (b * c) - d); { ERRO Parenteses de fechamento extra ) }
    a = a + (b * c) / ; { ERRO Operador sem operando a direita }
    a = a * (b + c)) / d; { ERRO Parenteses de fechamento extra ) }
    a = (a + b) * (c - d)) / e; { ERRO Parenteses de fechamento extra ) }
    a = a + (b + (c - d)); { Correto, mas incluido para verificar erro oculto }
    a = a * (b / (c + d) - e; { ERRO Parenteses de fechamento ausente }
    a = a + (b - c)) * d; { ERRO Parenteses de fechamento extra ) }
    a = a + (b * c) / (d - e; { ERRO Parenteses de fechamento ausente }
    a = a + (b * c)) / (d - e); { ERRO Parenteses de fechamento extra ) }
    {OLHAR} a = a + (b * c / d - e); { Correto, mas incluido para verificar erro oculto }
    a = a + (b * c / d - ); { ERRO Operador - sem operando a direita }
    a = a + (b * c / ) - d; { ERRO Operador sem operando }
    {OLHAR} a = a + () - d; { ERRO Expressao vazia dentro dos parenteses }
    {OLHAR} a = a + ( * c) - d; { ERRO Operador * sem operando a esquerda }
    {OLHAR} a = a + (b * ) - d; { ERRO Operador * sem operando a direita }
    {OLHAR} a = a + (b / c - d; { ERRO Parenteses de fechamento ausente }
    {OLHAR} a = a + b) * c - d; { ERRO Parenteses de abertura ausente }
    {OLHAR} a = a + (b * c) - d) + e; { ERRO Parenteses de fechamento extra ) }
    {OLHAR} a = a + ((b * c) - d; { ERRO Parenteses de fechamento ausente }
    {OLHAR} a = (a + b) * (c - d)) + e; { ERRO Parenteses de fechamento extra ) }
    {OLHAR} a = (a + b) * ((c - d) + e; { ERRO Parenteses de fechamento ausente }
    {OLHAR} a = a + (b * c)) / (d - e; { ERRO Parenteses de fechamento ausente }
    {OLHAR} a = a + b * (c - d)) / e; { ERRO Parenteses de fechamento extra ) }
    {OLHAR} a = a + (b * (c - d) / e; { ERRO Parenteses de fechamento ausente }
    {OLHAR} a = a + (b * c) / (d - e)) + f; { ERRO Parenteses de fechamento extra ) }
fimfuncao